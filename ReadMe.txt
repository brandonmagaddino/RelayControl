This was made in conjunction with the supplied code FTD2XX_NET. This should act as a Virtual Bridge for other languages on windows, this allows terminal support.

./release holds the released exe

./src should have all the source needed to build the project (Visual Studio 2010)